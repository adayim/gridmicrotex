# Resolve TeX font style flags to R font face

Resolve TeX font style flags to R font face

## Usage

``` r
resolve_font_face(font_style)
```

## Arguments

- font_style:

  Integer style flags from MicroTeX.

## Value

R font face string (plain, bold, italic, bold.italic).
